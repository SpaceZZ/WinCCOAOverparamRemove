# WinCCOAOverparamRemove
Removes overparamatrization from the panel of <b>Win CC Open Architecture</b> for all the instances of the objects (references) specified in the config.ini.

Uses the Pysimplegui as UI.
